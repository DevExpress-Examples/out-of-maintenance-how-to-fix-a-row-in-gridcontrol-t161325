<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid" x:Class="FixedRowExample.MainWindow"
    xmlns:local="clr-namespace:FixedRowExample"
    xmlns:dxb="http://schemas.devexpress.com/winfx/2008/xaml/bars"
    xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm"
        Title="MainWindow" Height="350" Width="525">
    <Window.DataContext>
        <local:TaskViewModel/>
    </Window.DataContext>
    <Grid>

        <dxg:GridControl x:Name="grid1" AutoGenerateColumns="AddNew" ItemsSource="{Binding TaskData}">
            <dxmvvm:Interaction.Behaviors>
                <local:FixedRow x:Name="fixedRowBehavior"/>
            </dxmvvm:Interaction.Behaviors>
            <dxg:GridControl.View>
                <dxg:TableView x:Name="tableView1">
                    <dxg:TableView.RowCellMenuCustomizations>
                        <dxb:BarButtonItem Content="Fix Row" x:Name="barButton1" ItemClick="barButton1_ItemClick"/>
                    </dxg:TableView.RowCellMenuCustomizations>
                </dxg:TableView>
            </dxg:GridControl.View>
        </dxg:GridControl>

    </Grid>
</Window>
